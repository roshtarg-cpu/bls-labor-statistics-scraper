{
  "actorSpecification": 1,
  "name": "bls-labor-statistics-scraper",
  "title": "BLS Labor Statistics Scraper — CPI, Jobs & Wages",
  "description": "Scrapes Bureau of Labor Statistics data: CPI, employment, wages, and unemployment series. Returns structured JSON for economists, analysts, and AI agents. Works with Claude, ChatGPT, and AI agents via Apify MCP.",
  "version": "0.1",
  "buildTag": "latest",
  "dockerfile": "./Dockerfile",
  "output": {
    "actorOutputSchemaVersion": 1,
    "fields": [
      {
        "name": "seriesId",
        "type": "string",
        "description": "BLS series ID",
        "template": "{{{seriesId}}}"
      },
      {
        "name": "year",
        "type": "string",
        "description": "Year of data point",
        "template": "{{{year}}}"
      },
      {
        "name": "period",
        "type": "string",
        "description": "Month period code",
        "template": "{{{period}}}"
      },
      {
        "name": "periodName",
        "type": "string",
        "description": "Month name",
        "template": "{{{periodName}}}"
      },
      {
        "name": "value",
        "type": "string",
        "description": "Data value",
        "template": "{{{value}}}"
      },
      {
        "name": "footnotes",
        "type": "string",
        "description": "Any footnotes for the data point",
        "template": "{{{footnotes}}}"
      },
      {
        "name": "seriesTitle",
        "type": "string",
        "description": "Human readable series title",
        "template": "{{{seriesTitle}}}"
      },
      {
        "name": "scrapedAt",
        "type": "string",
        "description": "Timestamp of scrape",
        "template": "{{{scrapedAt}}}"
      }
    ]
  }
}
